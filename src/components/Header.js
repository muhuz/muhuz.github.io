import React from 'react';

const Header = () => {
    return (
        <header className="navbar">
            <a href="/">mhz blog</a>
            <div>
                <a href="#">about</a>
                <a href="#">archive</a>
                <a href="#">tags</a>
            </div>
        </header>
    )
};

export default Header;